#include "main.h"
/**
 * _islower - function to print lowercase
 *
 * @c: parameter to be printed
 *
 * Return: 1 if it is lowercase
 * and 0 otherwise
 */
int _islower(int c)
{
	return (c >= 97 && c <= 127);
}
